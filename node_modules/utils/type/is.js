module.exports = (type, val) => ![, null].includes(val) && val.constructor === type;

//is(Array, [1]); // true
//is(ArrayBuffer, new ArrayBuffer()); // true
//is(Map, new Map()); // true
//is(RegExp, /./g); // true