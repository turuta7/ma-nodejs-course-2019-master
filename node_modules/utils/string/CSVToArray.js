module.exports = (data, delimiter = ',', omitFirstRow = false) =>
  data
    .slice(omitFirstRow ? data.indexOf('\n') + 1 : 0)
    .split('\n')
    .map(v => v.split(delimiter));

//CSVToArray('a,b\nc,d'); // [['a','b'],['c','d']];
//CSVToArray('a;b\nc;d', ';'); // [['a','b'],['c','d']];