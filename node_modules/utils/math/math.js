const { approximatelyEqual } = require('./');
const { average } = require('./');
const averageBy = require('./averageBy');
const { binomialCoefficient } = require('./');
const { clampNumber } = require('./');
const { degreesToRads } = require('./');
const { digitize } = require('./');
const { distance } = require('./');

module.exports = {
    approximatelyEqual:approximatelyEqual, 
    average: average, 
    averageBy:averageBy,
    binomialCoefficient:binomialCoefficient, 
    clampNumber:clampNumber,
    degreesToRads:degreesToRads, 
    digitize:digitize, 
    distance:distance
}  